# audio-helper — pip requirements (portable, no `file://` deps).
#
# For a plain install:
#     pip install -r requirements.txt
#     pip install -e .           # audio-helper itself (editable) or `pip install .`
#
# Optional surfaces live behind extras (not pulled by this file):
#     pip install '.[cli]'       # click-based CLI twin
#     pip install '.[api]'       # FastAPI HTTP surface (audio_helper.api)
#     pip install '.[api,mcp]'   # + MCP server (audio_helper.mcp)
#     pip install '.[demucs]'    # source-separation stems
#
# The os-helper pin is kept in lock-step with the release tag below.

# --- Suite dependency -----------------------------------------------------
os-helper>=1.4.2
# --- Core runtime ---------------------------------------------------------
ffmpeg-python>=0.2.0,<0.3
tqdm>=4.67.1,<5
scipy>=1.15.1,<2
numpy>=1.24
